# To ensure app dependencies are ported from your virtual environment/host machine into the container, run 'pip freeze > requirements.txt' in the terminal to overwrite this file
fastapi[all]
uvicorn[standard]
gunicorn

asyncpg
sqlalchemy-cockroachdb

sqlalchemy
sqlalchemy_utils
greenlet  # Required for SQLAlchemy async operations

pydantic
starlette
strawberry-graphql

# Microsoft Entra ID (Azure AD) Integration
msal==1.24.1
python-dotenv==1.0.0
itsdangerous==2.1.2  # For secure session cookies

# Redis for session storage (shared with frontendui)
redis==5.0.1

click
aiodataloader
aiohttp
requests
pyjwt[crypto]

https://github.com/hrbolek/uoishelpers/archive/refs/heads/main.zip

prometheus-client
prometheus_fastapi_instrumentator